NODE_ENV=test

# App
WEB_BASE_URL=http://localhost:3000
API_BASE_URL=http://localhost:3000
COOKIE_KEY=ci-test-cookie-key-not-for-production
UPLOAD_STORAGE_PATH=/tmp/uploads

# Database — hostname matches the service name in ci.yml
DATABASE_URL=postgres://hostr:hostr@database/hostr

# Redis — hostname matches the service name in ci.yml
REDIS_URL=redis://redis:6379

# MinIO/S3 — hostname matches the service name in ci.yml
# Credentials match MINIO_ACCESS_KEY / MINIO_SECRET_KEY in ci.yml services
AWS_ENDPOINT=http://minio:9000
AWS_ACCESS_KEY_ID=7HYV3KPRGQ8Z5YCDNWC6
AWS_SECRET_ACCESS_KEY=0kWP/ZkgIwQzgL9t4SGv9Uc93rO//OdyqMH329b/
AWS_BUCKET=hostr
